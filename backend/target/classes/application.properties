# ==============================================================
# LOCAL DATABASE CONFIGURATION (Default - H2 In-Memory)
# ==============================================================
# This allows you to test the Spring Boot app immediately without setup.
spring.datasource.url=postgresql://postgres.ixmyyrdmgxrgvurnvtqm:[9842315131@RK]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=RajKumar-008
spring.datasource.password=9842315131@RK
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# ==============================================================
# CLOUD DATABASE CONFIGURATION (PostgreSQL - FREE TIER)
# ==============================================================
# When you are ready to use a FREE Cloud Database like Supabase or Render:
# 1. Comment out the local H2 settings above.
# 2. Uncomment the lines below.
# 3. Replace the URL, username, and password with your cloud provider details.

# spring.datasource.url=jdbc:postgresql://db.YOUR_SUPABASE_URL.supabase.co:5432/postgres
# spring.datasource.username=postgres
# spring.datasource.password=YOUR_SECURE_PASSWORD
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Automatically create/update database tables based on Java models
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Port for Backend Server
server.port=8080
